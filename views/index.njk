{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
  <h1>{{ title }}</h1>

  {% for movie in movies %}
    <p>{{ movie.title }} {{ movie.score }}</p>

  {% endfor %}

</main>
{% endblock %}